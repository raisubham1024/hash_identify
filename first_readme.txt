## How to use ##

1. copy the code of the `hash_indentify.txt` file
2. open this hashcat hash example page -> https://hashcat.net/wiki/doku.php?id=example_hashes
3. open developer console on that page, and paste the text

'when you paste and enter, you see hash example with their length

## so what ? ##

like if your target hash text is something like -> "e3*******" 
you can go to developer console and then -> '<your_hash>'.length

#when you find the length, so then you, can CTRL+F to 'hashcat example page' console and find that length value their
#so you can easity see what the hash length are matched 